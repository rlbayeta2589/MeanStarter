npm install #installs the nodejs dependencies
# mysql -uroot -puser < lib/createuser.sql #creates the user username and the database DATABASE_NAME
# mysql -uusername -ppassword DATABASE_NAME < lib/dump.sql #creates the tables in the database DATABASE_NAME
